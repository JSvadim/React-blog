export interface HeaderNavbarComponentI {
    isNavOpened: boolean;
    toggleMenu: Function;
}

export interface BurgerMenuComponentI {
    className?: string;
    isNavOpened: boolean;
    toggleMenu: Function;
}